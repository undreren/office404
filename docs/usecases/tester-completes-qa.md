# Tester completes QA

## Given
A merged task with pending QA story points and a staffed tester.

## When
Time elapses until QA completes.

## Then (invariants)
- Task `testStoryPointsEarned` reaches `storyPointsRequired`
