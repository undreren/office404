# Deliver on time reputation bonus

## Given
A deliverable project with `lateCount` of 0.

## When
The player delivers the project.

## Then (invariants)
- Reputation increases by at least `ON_TIME_REP_BONUS`
